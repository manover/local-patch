#!/bin/sh

PDIR=/usr/share/#PACKAGE#/patches
#printf "#PACKAGE# prerm %s\n" "$*" >&2

unpatch_file() {
	patch -d / -p1 --no-backup-if-mismatch -f -r - -R -i $1
}

for p in $PDIR/*; do
	unpatch_file $p
done

#DEBHELPER#
exit 0
