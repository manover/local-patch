#!/bin/sh

PDIR=/usr/share/#PKG#/patches
#printf "#PKG# prerm %s\n" "$*" >&2

unpatch_file() {
	patch -d / -p1 --no-backup-if-mismatch -f -r - -R -i $1
}

for p in $PDIR/*; do
	unpatch_file $p
done

#DEBHELPER#
exit 0
